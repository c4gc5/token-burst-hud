# Token Burst HUD

Use this skill when creating, updating, packaging, or debugging the Token Burst HUD Codex++ plugin.

Rules:
- Prefer real token usage from host events or API `usage`.
- Mark estimated values with `estimated: true`.
- Keep all data local.
- Do not add analytics, remote scripts, or prompt upload.
